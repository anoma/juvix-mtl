module Mtl.MonadTrans;

import Stdlib.Prelude open;

trait
type MonadTrans (T : (Type -> Type) -> Type -> Type) :=
  mkMonadTrans@{
    lift : {A : Type} -> {M : Type -> Type} -> {{Monad M}} -> M A -> T M A;
  };

open MonadTrans public;
