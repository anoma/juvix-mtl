module Mtl.MonadReader;

import Stdlib.Prelude open;

trait
type MonadReader (S : Type) (M : Type -> Type) :=
  mkMonadReader@{
    {{monad}} : Monad M;
    ask : M S;
    local : {A : Type} → (S -> S) -> M A → M A;
  };

open MonadReader hiding {monad} public;
