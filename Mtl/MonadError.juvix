module Mtl.MonadError;

import Stdlib.Prelude open;

trait
type MonadError (Err : Type) (M : Type -> Type) :=
  mkMonadError@{
    monad : Monad M;
    throw : {A : Type} -> Err -> M A;
  };

open MonadError public;
